## 🛠️ Requirements

This project uses the following core dependencies:

- **tensorflow**: Provides the core deep learning framework for model creation, training, and saving/loading.
- **autokeras**: Enables automated machine learning (AutoML), allowing the script to generate and optimize neural network architectures.
- **googletrans==4.0.0-rc1**: Powers translation of model summaries and descriptions for multilingual support.

Install all dependencies with:
pip install -r requirements/requirements.txt